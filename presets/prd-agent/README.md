# PRD Agent

面向 MVP 的 PRD 生成、评审和修订 Preset。它包含两个私有 Skill，并只暴露需求
澄清、Skill 加载及工作区文件操作所需的最小工具集合。

从仓库根目录验证并安装：

```sh
npm run validate
npm run install:dsh:dry-run -- prd-agent
npm run install:dsh -- prd-agent
```

详细使用方法和行为约束见仓库根目录的产品说明。
