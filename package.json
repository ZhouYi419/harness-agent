{
  "name": "dsh-extension-kit",
  "version": "0.3.0",
  "private": true,
  "description": "An extensible catalog of presets, skills, plugins, workflows, and bundles for DeepSeek Harness.",
  "type": "module",
  "engines": {
    "node": ">=22.19"
  },
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "test": "npm run validate && node --test tests/*.test.mjs && node scripts/smoke-dsh.mjs",
    "smoke:dsh": "node scripts/smoke-dsh.mjs",
    "install:dsh": "node scripts/install.mjs",
    "install:dsh:dry-run": "node scripts/install.mjs --dry-run"
  },
  "license": "MIT"
}
